import { Navbar, Landing } from "@/components";
import { type NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div>
      <Navbar />
      <Landing />
    </div>
  );
};

export default Home;
